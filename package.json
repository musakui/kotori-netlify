{
	"name": "kotori-netlify",
	"description": "minimal ActivityPub instance",
	"author": "musakui",
	"license": "MIT",
	"private": true,
	"repository": "github:musakui/kotori-netlify",
	"version": "0.0.4",
	"type": "module",
	"scripts": {
		"ntl:dev": "netlify dev",
		"ntl:build": "netlify build",
		"deploy": "netlify deploy",
		"build:fn": "vite build --outDir dist-fn --ssr server.js",
		"build": "vite build",
		"dev": "vite"
	},
	"dependencies": {
	},
	"devDependencies": {
		"@musakui/fedi": "^0.0.11",
		"@musakui/kotori": "^0.0.5",
		"netlify-cli": "^12.2.0",
		"vite": "^3.2.4"
	}
}
